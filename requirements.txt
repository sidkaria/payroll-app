streamlit>=1.32.0
openpyxl>=3.1.0
# python-docx intentionally removed: it pulls in compiled lxml, which broke the
# Streamlit Cloud build/import. The .docx notes path is legacy and imported
# lazily (see parse_docx_notes); Megha uploads .xlsx. Re-add here only if .docx
# support is needed again.
